#include <stdio.h>

/**
 * main - a necessary function which enables the program to compile
 *
 * Return: 0 when successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
